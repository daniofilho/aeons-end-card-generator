import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
  // If you are using plugins like @vitejs/plugin-react or @vitejs/plugin-vue,
  // make sure to include them here inside a `plugins` array.
  // plugins: [react()],
  base: '/aeons-end-card-generator/',
});
